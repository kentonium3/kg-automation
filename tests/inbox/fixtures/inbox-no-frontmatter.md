Plain text note with no YAML frontmatter at all.

Just a free-form body. No --- delimiters anywhere. Must classify as
`unprocessed` per existing behavior (NOT as `parse_failure` — that's
reserved for malformed frontmatter, not absent frontmatter).
