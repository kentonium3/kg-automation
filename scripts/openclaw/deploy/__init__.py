"""Felix agent-prompt deploy pipeline.

Pulls agent prompt files from the kg-automation repo on office2 into the
deployed openclaw workspace dirs under /data/services/openclaw/<dir>/.

See `deploy_agent_prompts.py` for the helper module and CLI entry point.
"""
