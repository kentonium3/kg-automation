"""One-shot operator helpers for the OpenClaw runtime.

Mission #374 (main-verbatim-passthrough-01KSATRP) seeded this package
with ``rotate_main_session`` — a forced rotation of the main agent's
active sessions so a fresh ``AGENTS.md`` is re-loaded on the next
invocation.
"""
