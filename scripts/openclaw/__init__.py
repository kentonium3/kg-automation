"""OpenClaw operator/agent automation scripts.

This package collects the deterministic Python helpers that operate on
the OpenClaw runtime on office2 (agent registration, session lifecycle,
observation, enforcement). Sub-packages own one concern each.
"""

__version__ = "0.1.0"
