# Pinned runtime dependencies for the #849 arms (research.md §Supply chain).
# Installed into the repo venv by `substrate setup` and into the runner image.
# `openai` is listed by the task prompt; the client is urllib to /completion
# (WP01), so it is unused at run time — flagged for the design lead (WP01 review).
graphiti-core==0.30.2
falkordb==1.7.1
fastembed==0.8.1
openai==3.19.2
