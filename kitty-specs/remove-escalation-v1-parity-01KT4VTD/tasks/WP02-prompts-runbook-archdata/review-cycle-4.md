**Issue 1**: The runbook still fails T008's required validation grep. `grep -nE "Felix-Escalation|phantom_subscription|phantom subscription|parity" docs/runbooks/escalation-ops.md` currently matches lines 27 and 351 because WP02 added the mission slug/title wording containing `parity`. Remove or reword those references so the runbook is JSONL-current without parity terminology, then rerun the exact T008 grep.

**Issue 2**: Several active WP02 surfaces still document the removed transition instead of reading as if the v1 parity behavior never existed. Examples: `scripts/openclaw/skills/escalation/SKILL.md` lines 25-27, `scripts/openclaw/agents/felix-admin-escalation/AGENTS.md` lines 270-271, `docs/design/architecture/data-flows.md` lines 173/198/274, `docs/design/architecture/service-inventory.md` lines 308/319, and the matching JSON fields in `docs/design/architecture/data/*.json`. T007 explicitly says the post-edit prompt text should read as if v1 parity never existed, and T008 asks introductory framing to be "JSONL canonical." Replace the "removed transitional dual-write/comment/v1/parity" historical notes with current-state descriptions such as "JSONL is the sole canonical state source" and use `updated_by: "#376"` where metadata is needed.

**Issue 3**: The final T011 grep is not clean in this workspace. After the escalation cleanup, the remaining tracked matches are in enrichment surfaces (`scripts/enrichment/*`, `tests/enrichment/*`) for `_format_v1_comment`/`_COMMENT_PREFIX`, which appear to pre-exist on the base branch and may be outside this WP's intended escalation scope. Either adjust the final verification command/scope in the implementation notes if those enrichment matches are intentionally out of scope, or make the required active-surface grep pass before resubmitting.
