**Issue 1 (blocking - dead code)**: `scripts/common/vikunja_client.py` has no production caller. The targeted import/call-site search under `scripts/` returns zero hits outside the module itself, so anti-pattern check 1 fails. WP02 is described as the first consumer, but it is not implemented yet. Resolve the mission slicing by landing a live production caller with this client or by obtaining an explicit governance-approved exception/re-scoping before resubmitting WP01.

**Issue 2 (blocking - malformed URLs)**: `VikunjaClient._request()` always appends parameters with `?` (`scripts/common/vikunja_client.py:225-227`). A path that already contains a query string becomes `/tasks?filter=done?page=2`, so the added parameter is swallowed into the existing value. Compose the query with `urllib.parse` or select `&` when a query already exists, and add the WP-required embedded-query regression test.

**Issue 3 (blocking - empty response contract)**: Empty successful responses return `None` (`scripts/common/vikunja_client.py:250-251`), while T003 explicitly specifies `{}` and the data model describes DELETE as returning parsed JSON, often an empty dict. This is also an undocumented silent-empty return under anti-pattern check 3. Return `{}` and update the 204/DELETE assertions.

**Issue 4 (blocking - method header contract)**: `Content-Type: application/json` is added only when `json_body` is non-`None` (`scripts/common/vikunja_client.py:229-233`). The WP and contract require this header for POST and PUT. The test at `tests/common/test_vikunja_client.py:194-198` currently locks in the opposite behavior. Set the header based on the HTTP method and add coverage for bodyless POST and PUT.

**Issue 5 (blocking - FR coverage claim)**: WP01 frontmatter claims all of FR-012, but its tests cover only FR-012(d-f). There are no assertions for cardiac-task filtering, weekday percentage math, or a non-zero prior-week baseline (FR-012(a-c)); anti-pattern check 4 therefore fails as written. Re-scope the WP's requirement reference through Spec Kitty to only the client-specific FR-012 behaviors, or include the production path and real assertions for every claimed behavior.

**Verification**: `python3 -m pytest tests/common/test_vikunja_client.py --cov=scripts.common.vikunja_client --cov-branch --cov-fail-under=90 -q` passes: 40 tests, 100% line coverage, 100% branch coverage. The focused runtime probes reproduce Issues 2-4.
