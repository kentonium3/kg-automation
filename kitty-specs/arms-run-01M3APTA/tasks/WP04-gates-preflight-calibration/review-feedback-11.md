[MAJOR] scripts/research/arms849/litscan.py:170 — `_unordered` clears taint for `sorted`/`min`/`max` even when their result contains an unordered container: `"".join(sorted([{"or","acle"}])[0])` makes the actual gate FAIL under `PYTHONHASHSEED=0` but PASS under `3`; `min([{"or","acle"}])`, `max(...)`, and `min([], default={"or","acle"})` likewise bypass refusal — why: an in-scope literal construction still makes exclusion enforcement hash-seed dependent — fix: preserve nested/returned-container taint through these consumers, including `default`, and add both-seed regression tests.

Notes (non-blocking, Codex): 116 passed / 109 sandbox socket-fixture failures (not findings); extraction probes passed or refused correctly; real package gate "12 modules scanned (12 registered present), no hit"; the three frozenset re-wraps permitted; opaque constructs stayed opaque.

Scope note (orchestrator): inside the D-8 closure rule (a literal-only construction the taint analysis misclassifies) — folded. Source: Codex read-only review (gpt-6-astra), WP04 cycle 11 on lane-d @2ef05ff7, 2026-09-25; design-lead APPROVE on the fold stands (20260925T195446306945Zea50850d84). VERDICT: REJECT.
