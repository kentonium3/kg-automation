[MAJOR] scripts/research/arms849/litscan.py:192 — `"".join(sorted(map(frozenset, [["or","acle"]]))[0])` escapes taint tracking: `find_words` detects the word under PYTHONHASHSEED=0 but returns no hits under seed 3; `map(set, ...)` reproduces it — why: an explicitly in-scope literal-only construction makes the isolation scan seed-dependent — fix: propagate NESTED taint for unordered values produced by mapped callables, refuse their sorting/consumption, and add both-seed regressions.

Notes (non-blocking, Codex): other requested probes behaved consistently (map/zip/enumerate/reversed/dict.fromkeys into sorted, key=len, str.join on an allowlisted receiver, set algebra); real package gate "12 modules scanned, no hit"; 116 passed / 125 sandbox socket-fixture failures (not findings).

Scope note (orchestrator): inside the D-8 closure rule — a bare `set`/`frozenset` Name used as a VALUE (a callable handed to map/filter/sorted key=) constructs unordered containers the taint analysis did not see, because it only recognised Set literals and set()/frozenset() CALLS. Folded. Source: Codex read-only review (gpt-6-astra), WP04 cycle 12 on lane-d @cc6664d6, 2026-09-25. VERDICT: REJECT.
