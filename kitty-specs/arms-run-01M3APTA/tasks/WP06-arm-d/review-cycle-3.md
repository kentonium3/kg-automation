---
affected_files: []
cycle_number: 3
mission_slug: arms-run-01M3APTA
reproduction_command:
reviewed_at: '2026-09-25T04:47:27Z'
reviewer_agent: claude
wp_id: WP06
---

REOPEN for cycle 3 (fix cycle on an approved WP; the approve @f6f75e78 stands): Opus c2's six latent MINORs, each folded with a test.
[MINOR] scripts/research/arms849/arm_d.py:66-70,179-180 — the last-line re-wrap emits an exception whose message and fields are FALSE, and the docstring's "the row contract does not depend on that" overstates what I4 gets. Ran it: with a facade whose `complete` raises a bare `serving.ContextExceeded`, question C1 produces `ContextExceeded: prompt is 51425 tokens; trained limit 262144 — exceeds_model_context, not sent`, `limit=262144`, `limit_applied="trained"`, and `prompt_tokens > model_context_tokens` is **False** — why: data-model I4 is "prompt_tokens present on every exceeds_model_context row AND > model_context_tokens"; on this path only the first half can hold, and `context_limit_applied` names the trained limit when the permitted limit is what actually refused, so the row asserts something untrue. Latent — the window is 260,096–262,144 and no question lands in it (nearest A 139,517 and F1 273,024) — and the re-raise itself is blessed by the contract's 2026-09-25 note, so this is the claim, not the mechanism — fix: soften to the presence half of I4 (the "> model_context" half is met only on the gate path), or have the re-wrap carry the cause's own limit rather than the gate's.
[MINOR] scripts/research/arms849/arm_d.py:178 — `except ContextExceeded: raise` is the ONLY executable statement in the module the suite never reaches (measured: a `trace` run over all 16 tests leaves lines 111/142/147/155/203 — all docstrings — and 178; every other line, including the two that were dead at cycle 1, is now covered) — why: it is load-bearing, not decoration: without it the subclass falls into the `serving.ContextExceeded` handler below and is re-wrapped with the gate's numbers, silently discarding a more specific exception; a regression there would be invisible — fix: one test whose facade's `complete` raises `D.ContextExceeded` and asserts the identical object propagates (`info.value is raised_obj`).
[MINOR] tests/research/test_arms849_arm_d.py:155,194 — the measured figures are still invisible on a default run: `pytest -q tests/research/test_arms849_arm_d.py` without `-s` emits zero `MEASURED` lines (verified, grep -c = 0); they appear only when the runner remembers `-s` — why: cycle 1's point was that WP10's re-measurement can cite these eight numbers off a green run, and a green run in CI or under the mission's own invocation still emits none — fix: write them to a JSON artifact under the run's build dir (a `tmp_path_factory`/build-dir file), which survives capture and can be cited.
[MINOR] scripts/research/arms849/arm_d.py:154,173 — the arm takes `ctx.limit` / `ctx.limit_applied` wholesale and never checks that the pair is coherent; T026 step 2 and contracts/arm-interface.md put the D-11 choice in the arm (`ctx.limits.trained` when primary else `ctx.limits.permitted`) — why: the rule is in fact owned and tested one layer down (`ServingConfiguration.limit_applied()`, asserted at tests/research/test_arms849_serving.py:38-39) and approved arm G uses the same facade, so this is safe today — but nothing in arm D would notice a WP08 `CellContext` that paired `limit_applied="trained"` with the permitted number, and `context_limit_applied` is the D-only ledger column the six-of-eight finding is read off — fix: one line before the gate asserting `ctx.limit_applied in {"trained","permitted"}` and, where the config is reachable, that the pair equals `limit_applied()`.
[MINOR] scripts/research/arms849/arm_d.py:119 — the empty-view guard is disjunctive on records (`not (view.entities or view.edges)`), so a view with events and entities but ZERO EDGES is accepted: I called `render_dump` with `Loaded(events=…, entities=…)` and got counts `(5750, 50, 0)` and a block, no complaint — why: zero edges in D's dump is precisely Codex blocker H-1, the defect this mission exists to have fixed, and at runtime it would score a cell rather than refuse; only the eight real views are protected, by `assert nd > 0` at tests:229 — fix: judgment call (a genuinely edge-free view is conceivable early in a corpus), so either require edges as well or record in the guard's comment why zero edges is tolerated where zero records is not.
[MINOR] scripts/research/arms849/arm_d.py:105 and tests/research/test_arms849_arm_d.py:290-294 — a view object with no `links` attribute raises `AttributeError`, which contracts/arm-interface.md class (3) routes to the infrastructure ladder (health check + ≤ 2 retries), while the same defect with a non-empty `links` raises `ArmRefusal` and is terminal with zero retries; the test pins the `AttributeError` — why: one defect class, two outcomes, and the retried one burns a ~28-minute primary attempt each time; unreachable in practice, since `Loaded` is a dataclass whose fields are `ask_time, events, entities, edges, links, withheld` (verified), so it is cosmetic — fix: `getattr` with a sentinel and raise `ArmRefusal("view has no links attribute…")`.
