---
affected_files: []
cycle_number: 1
mission_slug: arms-run-01M3APTA
reproduction_command:
reviewed_at: '2026-09-25T04:32:48Z'
reviewer_agent: claude
wp_id: WP06
---

VERDICT: APPROVE with eleven MINORs — Opus fallback review cycle 1 (reviewer-renata, model opus; Codex out of credits) on lane-f b5584880, 2026-09-25. Independent re-measurement reproduced the six/zero split and B2 = 363,004. The minors are folded as cycle 2 before the approve is recorded (mission discipline: every finding gets a fix and a negative test):
[MINOR] scripts/research/arms849/arm_d.py:126 — `arm_d(question, view, ctx, text)` takes a fourth positional argument, but contracts/arm-interface.md declares `arm(question, view, ctx) -> Answer` and WP06 T026 step 1 writes `arm_d(question, view, ctx) -> Answer`; arm G conforms by holding `self.text` — why: WP08 builds ONE CellContext for both arms and will have to special-case D or invent `ctx.text`, and the deviation is undocumented in the file — fix: bind it (`functools.partial(arm_d, text=text)` already works because the parameter is named) and say so in the module docstring, or read the corpus text off `ctx`.
[MINOR] scripts/research/arms849/arm_d.py:109 — the byte-equality guard `block.data != events + records` is never executed by the test suite (verified with a `trace`-based coverage run of the 12 tests); all three cases in test_render_dump_refuses_a_reordered_or_incomplete_full_view trip an earlier guard (refs mismatch, keys mismatch, or the startswith check) — why: this is the one guard that catches a reordering which preserves refs AND keys AND the section boundary, i.e. exactly the failure trap 3 names, and it is shipping untested — fix: add a case that rotates the record lines while keeping `record_keys` identical (I ran it; the guard fires with "dump bytes are not event section + record section").
[MINOR] scripts/research/arms849/arm_d.py:148-154 — the post-complete telemetry loop and the `client_prompt_tokens` cross-check are also never executed (same trace run) and are unreachable through the real serving module: `Completion` is a frozen dataclass whose fields are all required and map_timings already refuses a client/server mismatch — why: they read as a live safety net in the docstring and in T026 step 3 but currently assert nothing that can fail, so a regression in them would be invisible — fix: either drive them with a stub facade that returns a Completion-shaped object with a None field and a mismatched client count, or drop them and cite serving's guarantee.
[MINOR] scripts/research/arms849/arm_d.py:95,110 — render_dump accepts an EMPTY view: I called it with `Loaded(ask_time=…)` and it returned a zero-byte block, counts (0,0,0) and the sha256 of the empty string with no complaint; the arm would then score an `ok` row whose "full dump" is nothing — why: every layout assertion passes vacuously on an empty view, so a loader/replay regression that returns nothing produces a scored cell instead of an error — fix: refuse a view with zero events (or zero records) in render_dump, since D is by definition the whole replay-visible view.
[MINOR] scripts/research/arms849/arm_d.py:45-48 — the ContextExceeded docstring claims subclassing serving.ContextExceeded makes the harness catch "one type for both the arm's gate (first line) and complete's own guard (last line)" and that "this one carries what the D row records … on every D row, scored or not"; the second half is not true of the source — serving.complete (serving.py:411-412) raises the BASE exception with only a message, so a refusal from the last line carries no prompt_tokens, no limit_applied and no plan, and data-model I4 ("prompt_tokens present on every exceeds_model_context row") would be unmet — why: the two limits differ on the primary (gate 262144 trained vs complete's permitted 260096), so there is a 2048-token window where the last line fires and the row loses its required fields. I measured every question: none lands in that window (nearest is A at 139517 and F1 at 273024), so it is latent, not live — fix: soften the claim to "the gate is the only path that can fire for this corpus", or have the harness re-wrap a bare serving.ContextExceeded with the plan it already holds.
[MINOR] scripts/research/arms849/arm_d.py:63-64 — the PlanRecord docstring attributes "assembled context excludes the fixed prompt and the question" to rubric §5; that sentence is not in §5 (it is in the WP06 prompt). The authority for the field is data-model.md § Row run, "the slot's tokens only — the cost primitive" — why: the code is right, the citation is not, and §2/§5 citations in this mission are load-bearing for WP10 — fix: cite data-model § Row run.
[MINOR] tests/research/test_arms849_arm_d.py:153,192 — the measured figures are recorded only in an assertion message (invisible on a passing run) except B2, which is a `print` that needs `-s`; the other seven counts are never surfaced — why: the WP asks that WP10's re-measurement be able to cite these numbers, and a green run currently emits none of them without `-s` — fix: emit all eight through a `print` or a small JSON artifact under the run's build dir.
[MINOR] tests/research/test_arms849_arm_d.py:314-316 — `by_key` is built and never used — why: dead code in a test whose point is that the bytes are reconstructed independently; a reader may think it is part of the comparison — fix: delete it.
[MINOR] scripts/research/arms849/arm_d.py:82 — `getattr(view, "links", None)` means an object with no `links` attribute at all passes the refusal (confirmed: a stand-in view without the attribute is accepted) — why: the guard is meant to be "D reads no links", and absence should be as suspicious as presence for a type that always has the field — fix: `view.links` directly, or refuse when the attribute is missing.
[MINOR] scripts/research/arms849/arm_d.py:138,83 — both permanent configuration refusals raise plain ValueError, which contracts/arm-interface.md routes to "infrastructure failure -> health check -> retry <= 2 -> error" — why: a cache_prompt-off configuration or an un-narrowed view will be retried three times before being recorded, burning a ~28-minute cell on the primary — fix: a distinct exception type the harness treats as terminal (same class of change as ContextExceeded).
[MINOR] scripts/research/arms849/arm_d.py:155-166 — the returned row carries no `truncated`; data-model § Row run says `finish_reason == "length"` sets `truncated: true`, and contracts/grading-view.md requires `truncated` per view entry — why: derivable from finish_reason, and arm G omits it too, so this is a consistency note for WP08 rather than a defect here — fix: nothing in WP06; make sure WP08 derives it.
