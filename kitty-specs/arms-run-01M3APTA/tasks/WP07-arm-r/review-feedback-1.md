[MAJOR] scripts/research/arms849/arm_r.py:119 — Index validation compares only refs, and bind caches by question ID: probes accepted a changed ask_time and changed frozen event bytes with the original index — why: an index from another view is not refused, allowing retrieval using vectors inconsistent with inserted bytes — fix: store and validate ask_time plus a frozen-content fingerprint on EventIndex and use that identity in the cache key.
[MAJOR] scripts/research/arms849/arm_r.py:138 — The specified calibration calls fail: r_tokens_for(question, 5, tokenizer) raises TypeError, and availability_cap(question) raises AttributeError; no calibration adapter exposes those signatures — why: WP04 cannot invoke the promised T029 interface to determine the registered k — fix: expose bound calibration callbacks with the specified signatures, resolving each question's view and index internally.
[MINOR] scripts/research/arms849/arm_r.py:149 — A calibration object without a k attribute raises AttributeError instead of ArmRefusal, reproduced with SimpleNamespace() — why: a configuration defect enters infrastructure retry handling instead of terminal refusal — fix: retrieve the attribute with a missing-value sentinel and route it through the existing ArmRefusal validation.

Source: Codex read-only review (gpt-6-astra), WP07 cycle 1 on lane-g @e76532c5, 2026-09-25. Codex confirmed: 11 tests pass; deterministic vectors; records block byte-identical to D's; exact token counts; clamping above 772 events; assembly follows replay position (a literal (at, ref) sort would differ on C1 and violate A4). Orchestrator note on MAJOR 2: WP04's real `calibrate(ledger, availability: Mapping[str,int], assemble_r_tokens: Callable[[str,int],int])` is the binding shape; the fix is a bound adapter from R that yields exactly those two inputs per question (view + index resolved internally, index cached per question), not a change to WP04. VERDICT: REJECT.
