[MAJOR] scripts/research/run_849_harness.py:1042 — A preflight containing `[]` or `null` raises uncaught AttributeError inside load_preflight before the object check; reproduced on host, gates, fresh and resumed paths — why: bypasses exit 3, the gate failure record and resumed session_gates event — fix: translate the loader’s non-object failure into PreflightUnusable and test each CLI path.
[MAJOR] scripts/research/run_849_harness.py:1060 — With a matching preflight and setup.json containing `"cache_shas":[]`, identity_from_setup raises uncaught AttributeError on all four CLI paths — why: violates the promised ConfigUnavailable refusal with exit 1 and no traceback — fix: validate cache_shas as an object before identity construction and translate invalid structure into ConfigUnavailable.
[MINOR] scripts/research/run_849_harness.py — (design lead rider, bus 20260925T224715800432Zee5c206cc7) the container phase re-probes /props: if it exposes the served model's chat template, compare its sha to the cached tokenizer's and FAIL the gate on mismatch; if not exposed, record `chat_template_cross_check: could_not_check` with the reason — never silence.

Design-lead delta read @42595cf1: APPROVE; tokenizer confirmed as the chat-template authority (the pre-c4 comparison of the preflight against a value taken from the preflight could never fail — same family as W8-1's string half). Codex confirmed: independent sha mutations refused; the real cached-tokenizer positive case passes; ServingFacade still delegates to serving.complete without skipping its count (C11 untouched as ruled).
Process record: the first two c4 launches failed with Codex 401 (credential rejected); Kent re-logged via `codex login --device-auth`; this is the completed run. Source: Codex read-only review (gpt-6-astra), WP08 cycle 4 on lane-h @42595cf1, 2026-09-25; 138 passed. VERDICT: REJECT.
