---
affected_files: []
cycle_number: 11
mission_slug: arms-run-01M3APTA
reproduction_command:
reviewed_at: '2026-09-25T04:25:18Z'
reviewer_agent: claude
wp_id: WP03
---

VERDICT: REJECT — Opus fallback review cycle 10 (reviewer-renata, model opus; Codex out of credits) on lane-c 488938f4, 2026-09-25. c9's fullmatch fix confirmed by running code. Findings:
[MAJOR] scripts/research/arms849/ledger.py:312-316 — `AttemptsExhausted` is unreachable dead code and a 4th attempt raises `SecondScoredRow` instead — WP03 T012.1 states verbatim "raises `AttemptsExhausted` if that would exceed 3 (FR-007; D-12)"; the class is exported in `__all__` with the docstring "A fourth attempt was requested for a key", a claim untrue of the source. `terminal()` returns "error" once attempts_for >= MAX_ATTEMPTS, so the guard at :312 always fires before `n > MAX_ATTEMPTS` at :315 can; reproduced for both three-error and three-bare-start keys, and the test at tests/research/test_arms849_ledger.py:144,294 asserts `SecondScoredRow` — i.e. it was written to the implementation, not to T012.1. WP08 will reasonably write `except AttemptsExhausted` and never catch it — fix: raise `AttemptsExhausted` when `attempts_for(key) >= MAX_ATTEMPTS` and no non-error terminal outcome exists, reserve `SecondScoredRow` for a key with an actual terminal ok/exceeds/not_implemented row, and assert both types in the test.
[MAJOR] scripts/research/arms849/ledger.py:596-597 — the resume comparison covers only `Binding.__dataclass_fields__`, so the Header fields `blinding_seed` and `plan` are never compared — reproduced: a ledger written with blinding_seed=7, plan=72 resumes clean when open_ledger is handed blinding_seed=999999, plan=24, and the caller's values are silently discarded in favour of the header's. WP03's own reviewer guidance is explicit: "a new header field that is not compared is a silent hole; assert in a test that every `Header` field is in the compared set" — but tests/research/test_arms849_ledger.py:96,103 parametrise over `Binding.__dataclass_fields__`, so the test whose docstring claims "a new field cannot go uncompared" is structurally blind to exactly the fields that are uncompared. `plan` is the field that says whether this ledger is the 72-cell primary or the 24-cell secondary. Fix: compare every Header field except `record`/`started` (or fold blinding_seed and plan into Binding), and parametrise the refusal test over `Header.__dataclass_fields__`.
[MAJOR] scripts/research/arms849/ledger.py:177-178 — the corpus binding is computed-and-stored and a registered file that is ABSENT is silently dropped from it — `{name: fingerprint(...) for name in REGISTRATION["files"] if (corpus_dir/name).exists()}` never compares the content to `REGISTRATION["files"]`, the invariant data-model.md §Ledger states for this field ("must equal `REGISTRATION.files`"). Reproduced: (a) a tampered stream.jsonl binds as 7943c520… against the registered 188b9bf1… with no complaint; (b) an EMPTY corpus dir yields `binding.corpus == {}`, and that ledger both creates and resumes cleanly — a header that binds no corpus at all, in a module whose docstring says it "binds the ledger to one corpus". The same file WP03 imports from already ships the correct primitive, `load_849_corpus.verify_registration()`, whose docstring reads "A missing file is a mismatch… a corpus with a file missing is not the frozen one"; it also catches a scaled corpus via manifest.json `valid_for_run`. (The gate phases in contracts/gates.md verify fingerprints too, which limits blast radius for a gated run — but the ledger's binding is the record the analysis trusts, and its completeness must not depend on which files happen to exist.) Fix: call `verify_registration(corpus_dir)` and bind its returned observed map.
[MINOR] scripts/research/arms849/ledger.py:163,190 — the header's `limit_applied` is an unvalidated free string and is never cross-checked against D rows' `context_limit_applied` — reproduced: a header binding `limit_applied="banana"` is accepted, and a D `ok` row carrying `context_limit_applied="permitted"` appends under a header declaring "trained". Row-level `context_limit_applied` IS validated against CONTEXT_LIMITS at :355, so one file holds two standards for the same D-11 fact — fix: validate `limit_applied` against CONTEXT_LIMITS at binding construction and require D rows to agree with the header.
[MINOR] scripts/research/arms849/ledger.py:579 — a fresh ledger's `_rows` contains the header row; a resumed one does not (`Ledger(path, header, rows[1:], fd)` at :605) — reproduced: `led.rows[0]["record"] == "header"` on creation, `"attempt_start"` after reopen, and the row counts differ by one for the same file. Every internal consumer filters by `record`, so nothing is wrong today, but `rows` is a public property and WP04/WP08 read it — fix: append the header line without pushing it into `_rows`, or include it on both paths.
[MINOR] scripts/research/arms849/ledger.py:374 — `assembled_context_sha256` is validated through `str(...)` but persisted unconverted, so a non-string is accepted and stored as a non-string — reproduced: `row["assembled_context_sha256"] = int("1"*64)` passes `re.fullmatch(r"[0-9a-f]{64}", str(...))` (a 64-digit decimal is 64 hex characters) and the row persists an `int`. NFR-005/D-15 assert this digest equal across repeats and resumes; after a JSON round-trip `1111… (int)` and `"1111…" (str)` compare unequal. `_require_sha256` at :94 gets this right with `isinstance(value, str)` — fix: apply the same `isinstance(..., str)` guard here (and drop the `str()` coercion).
[MINOR] scripts/research/arms849/ledger.py:446-448 — the `summarise` docstring says every non-scored key is "COUNTED by its terminal outcome (`pending` when none yet)", but no `pending` count is ever emitted — reproduced: a cell with one in-flight attempt_start and no run row yields `counts == {}`, indistinguishable from a fully scored cell's empty counts. Fix: either emit `counts["pending"]` for a non-terminal key with no run rows, or delete the clause from the docstring.
[MINOR] scripts/research/arms849/ledger.py:320 — an R `ok` row is accepted with no `calibration` record present — contracts/ledger-schema.md §6 says "Every R cell requires it" and D-10 makes calibration the precondition for the R arm; reproduced by recording R/A/1 `ok` while `calibration()` is None. Arguably WP04/WP08's gate (the contract heads "Enforced by run_849_harness"), but the ledger already owns write_calibration's exactly-once and population rules — fix: refuse an R run row until a calibration record exists, or state the split in the docstring as T012.5 does for the timeout.
