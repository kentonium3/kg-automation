# Issue matrix — retire-private-folder-guards-01KY2MNK

Per FR-037 of the spec-kitty-mission-review skill Gate-4. One row per issue referenced in spec.md.

| Issue | Title | Verdict | Evidence ref |
|-------|-------|---------|--------------|
| #696 | Second-brain graph-layer privacy gate (ingest exclusion of `_private`) | deferred-with-followup | WP07 reframes the #696 privacy *model* to "verify not present" (physical exclusion) in `second-brain-graph-layer.md` + `executive-assistant-architecture.md`; the runtime ingest-time check is out of scope (pipeline not built) and remains #696's own follow-up. |

Valid `Verdict` values: `fixed`, `verified-already-fixed`, `deferred-with-followup`, `in-mission` (being fixed by a later WP in this mission; must reach a terminal verdict before mission `done`).
