# Issue matrix — restore-whatsapp-dm-reply-delivery-01KTVVHH

Per FR-037 of the spec-kitty-mission-review skill Gate-4. One row per issue referenced in spec.md.

| Issue | Title | Verdict | Evidence ref |
|-------|-------|---------|--------------|
| #588 | WhatsApp DM reply delivery silently fails — main/subagent generate replies but channel never sends | deferred-with-followup | This mission's source issue. WP01 verdict was `Fix shape: H6 — upgrade openclaw 2026.5.28 → 2026.6.5` (commit `d6b5d2da`). WP02 built the upgrade-path deploy script (commit `ef967cf0`). WP05 executed the upgrade cleanly (operator-driven sudo at 2026-06-12 ~01:30 UTC) and ran the 1-DM smoke at `2026-06-12 01:31:42 UTC` — **smoke FAILED with identical bug signature on 2026.6.5**: `embedded_run:started` fires, never reaches `:ended`, `stuck session recovery: action=abort_embedded_run` fires after 379s, no `[whatsapp] Sending message` event. Per FR-009 + C-001, mission escalated to H1 (vendored runtime regression) and filed internal tracking issue **kentonium3/kg-automation#589** with the full pre/post-upgrade evidence + CHANGELOG mapping that didn't pan out. Full smoke evidence: `docs/diagnostics/restore-whatsapp-dm-reply-delivery-01KTVVHH-smoke.md`. #588 stays open with comment linking #589 as the active tracker; the openclaw 2026.6.5 upgrade is retained on office2 (database-first refactor + other improvements are wins independent of #588). |
| #579 | Felix main agent stops relaying subagent replies — main/AGENTS.md truncation | verified-already-fixed | Closed via squash merge 37b3bf56 (2026-06-11). This mission's spec FR-006 explicitly preserves the #579 fix (main/AGENTS.md stays under 12K cap; felix-admin-calendar stays cleanly registered). WP01 H3 hypothesis was a rollback probe to verify the post-#579 AGENTS.md isn't the cause of #588 — WP01 verdict ruled H3 out (skipped per orchestrator since H6 desk verdict was unambiguous at the time). #579 fix remains in force; no further action required by this mission. SC-006 verified post-upgrade: zero `truncating in injected context` warnings on `agent:main:*` sessions. |
| #557 | Rebaseline obligation for audited surfaces | verified-already-fixed | #557 established the rebaseline framework (`docs/runbooks/security-baseline-ops.md`, `docs/design/architecture/data/audited-surfaces.json`, CI soft-reminder, merge-commit trailer requirement). This mission COMPLIES with the standing obligation: C-003 in spec.md, WP05 evidence file documents the rebaseline as a pending operator step (the openclaw upgrade DID change audited surfaces; the rebaseline command is canonical and must run before mission merge). The framework itself is not modified by this mission; it is honored. No follow-up required. |
| #589 | Vendored openclaw embedded_run lifecycle never completes for DM-initiated runs — H6 (2026.5.28 → 2026.6.5 upgrade) did NOT fix #588 | fixed | Filed by this mission as the FR-009 escalation deliverable. The deliverable IS the filed issue — opening this tracker (with full evidence: pre/post-upgrade journal slices, Codex CHANGELOG mapping that did not pan out, retained upgrade context, operator-pending upstream-filing decision) is the unit of work this mission satisfies. Issue exists at https://github.com/kentonium3/kg-automation/issues/589 (verified open as of 2026-06-12). Going forward, #589 is the active tracker for the residual vendored-runtime regression; operator decides whether to file upstream openclaw based on this evidence. |

Valid `Verdict` values: `fixed`, `verified-already-fixed`, `deferred-with-followup`, `in-mission` (being fixed by a later WP in this mission; must reach a terminal verdict before mission `done`).

## Notes

- `#588` verdict was originally `in-mission` (during WP01–WP04 approvals); transitioned to `deferred-with-followup` at WP05 when the 1-DM smoke failed and FR-009 escalation triggered. Per the spec's Decision D7 (research §9) the upgrade itself is retained as a win; the residual bug is tracked at #589.
- `#589` verdict is `fixed` per the spec-kitty rule that "the deliverable IS the filed issue" for mission-created tracker issues. The mission's unit of work for this issue was its filing (with full evidence + operator-approved title/body); no future code-edit work on this mission is required for that.
- The merge-commit trailer must record `Rebaseline: completed at <ISO8601-UTC>` once the operator runs the canonical command (per `audited-surfaces.json`). The smoke evidence file documents this as a pending step.
