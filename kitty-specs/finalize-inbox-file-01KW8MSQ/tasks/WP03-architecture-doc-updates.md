---
work_package_id: WP03
title: Required architecture/doc updates (agent-prompt-changed doc-map class)
dependencies:
- WP01
requirement_refs:
- FR-006
tracker_refs: []
planning_base_branch: feat/finalize-inbox-file-v2
merge_target_branch: feat/finalize-inbox-file-v2
branch_strategy: Planning artifacts for this mission were generated on feat/finalize-inbox-file-v2. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/finalize-inbox-file-v2 unless the human explicitly redirects the landing branch.
subtasks:
- T010
- T011
- T012
- T013
agent: "claude"
shell_pid: "79495"
history:
- date: '2026-06-28'
  note: 'Generated by /spec-kitty.tasks for #325'
agent_profile: curator-carla
authoritative_surface: docs/design/architecture/
create_intent: []
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- docs/design/architecture/data/service-inventory.json
- docs/design/architecture/service-inventory.md
- docs/design/architecture/data/audited-surfaces.json
- docs/runbooks/openclaw-agent-setup.md
- docs/runbooks/agent-prompt-sync-ops.md
role: implementer
tags: []
---

# WP03 — Required architecture/doc updates

## ⚡ Do This First: Load Agent Profile

```
/ad-hoc-profile-load curator-carla
```

You are **curator-carla**, a knowledge-base and documentation maintenance
specialist. Honor that profile's boundaries for this work package.

## Objective

Land the architecture/doc updates required by the `signal-to-doc-map.json` change
class **`agent-prompt-changed`** (this mission edits the `felix-admin-capture`
AGENTS.md). Keep the machine-readable record (JSON) authoritative and the narrative
markdown in sync. **Do not over-document** — touch a doc only where the finalize
contract change is genuinely reflected; otherwise record "reviewed, no change
needed" in your summary.

Depends on WP01 (the contract being documented must exist).

## Context

- Doc-map entry `mission-agent-prompt-changed` lists these targets:
  `service-inventory.json` + `service-inventory.md`, `audited-surfaces.json`,
  `openclaw-agent-setup.md`, `agent-prompt-sync-ops.md`.
- **Rebaseline (#557)**: the merge commit must record `Rebaseline: not required —
  agent-prompt-sync surface is not hashed by audit.sh (gap #621)`. You do not run a
  rebaseline; you record the reasoning. (Background: `audit.sh` hashes
  `openclaw.json` into `openclaw-config.txt` but never the agent `AGENTS.md` files,
  so the agent-prompt surface is unmonitored — there is nothing to rebaseline.)
- Machine-readable JSON is authoritative; markdown is the narrative view (when they
  conflict, JSON wins). There is a blocking architecture-data validator
  (`validate_architecture_data.py`) — keep the JSON schema-valid.
- See [spec.md](../spec.md) "Architecture Impact", FR-006.

## Branch Strategy

- **Planning/base branch**: `feat/finalize-inbox-file-v2`
- **Final merge target**: `feat/finalize-inbox-file-v2`
- **Depends on**: WP01 — branch from its base; read `lanes.json` for the worktree.

## Subtasks

### T010 — `service-inventory.json` felix-admin-capture entry (FR-006)

**Steps**:
1. Locate the `felix-admin-capture` agent entry (grep the JSON).
2. Update its `notes` (and `depends_on` if such a field exists) to reflect that the
   per-file finalize is now an **error-surfacing** call into
   `scripts/inbox/mark_processed.py` with a `0/1/2/3` exit contract (a failed
   finalize is surfaced, not silent). Add a `depends_on`/related reference to
   `scripts/inbox/mark_processed.py` only if the schema models script dependencies;
   otherwise keep it in `notes`.
3. Do not invent schema fields. Match the existing entry shape exactly; keep it
   schema-valid.

**Validation**: `python3 tooling/scripts/validate_architecture_data.py` (or the
canonical validator path) passes; the entry reads accurately.

### T011 — `service-inventory.md` narrative counterpart (FR-006)

**Steps**: Update the felix-admin-capture narrative row/section in
`service-inventory.md` to match the JSON (error-surfacing finalize, in-place). Keep
it brief; JSON remains authoritative.

### T012 — `audited-surfaces.json` review (FR-006)

**Steps**:
1. Read the felix-admin-capture / agent-prompt surface mapping.
2. Per known gap #621, the agent `AGENTS.md` surface is **not** hashed by
   `audit.sh`. Confirm the mapping is accurate/current. If it already correctly
   reflects this, record "reviewed, no change" — **do not fabricate** a mapping or a
   baseline. Only correct a genuine inaccuracy.

**Validation**: JSON schema-valid; no invented mappings.

### T013 — Runbook review (`openclaw-agent-setup.md`, `agent-prompt-sync-ops.md`) (FR-006)

**Steps**:
1. Skim both runbooks for any description of the felix-admin-capture finalize step
   or its deploy path.
2. Update only if the finalize-step contract / exit handling is described there
   (e.g., add a one-line note that finalize now surfaces errors via exit code). If
   not described, record "reviewed, no change needed".
3. Where relevant, confirm the slug→dir mapping (`felix-admin-capture` →
   `inbox-agent`) and the rebaseline-not-required reasoning are consistent.

**Validation**: any edits are accurate and minimal; reasoning for the
rebaseline-not-required note is captured for the merge commit.

## Definition of Done

- [ ] `service-inventory.json` felix-admin-capture entry reflects the error-surfacing
      in-place finalize; schema-valid; validator passes.
- [ ] `service-inventory.md` narrative matches the JSON.
- [ ] `audited-surfaces.json` reviewed; corrected only if genuinely inaccurate; no
      fabricated mapping/baseline.
- [ ] Both runbooks reviewed; updated only where the finalize contract is described.
- [ ] Rebaseline-not-required reasoning (gap #621) captured for the merge commit.

## Risks

- **Over-documentation** — the temptation is to touch every listed doc; only edit
  where the change is genuinely reflected. Record "reviewed, no change" otherwise.
- **Validator break** — keep all JSON schema-valid; run the architecture-data
  validator before declaring done.
- **Fabrication** — do not invent audited-surface mappings or baselines (gap #621
  means there is genuinely nothing hashed for agent prompts).

## Reviewer Guidance

Confirm JSON stays authoritative and schema-valid, the narrative matches the JSON,
no fabricated audited-surface entries were added, and the rebaseline-not-required
reasoning is recorded. Verify edits are minimal and accurate, not doc-bloat.

## Activity Log

- 2026-06-29T04:01:26Z – claude – shell_pid=79495 – Assigned agent via action command
- 2026-06-29T04:09:23Z – claude – shell_pid=79495 – service-inventory + audited-surfaces (#621 correction); validator OK; commit 6842733e
- 2026-06-29T04:20:03Z – user – shell_pid=79495 – Codex APPROVE after fix (2963026a): #621 runbook correction, exit-3, metadata
