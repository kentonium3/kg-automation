---
work_package_id: WP01
title: Author capture SOUL/USER/TOOLS + AGENTS label receiver (pure refactor, zero behavior change)
dependencies: []
requirement_refs:
- FR-001
- FR-002
- FR-003
- FR-004
- FR-005
- FR-006
- FR-007
- FR-008
- FR-009
- FR-010
- FR-011
tracker_refs: []
planning_base_branch: feat/author-capture-workspace
merge_target_branch: feat/author-capture-workspace
branch_strategy: Planning artifacts for this mission were generated on feat/author-capture-workspace. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/author-capture-workspace unless the human explicitly redirects the landing branch.
subtasks:
- T001
- T002
- T003
- T004
- T005
- T006
agent: "codex:gpt-5:reviewer-renata:reviewer"
shell_pid: "19998"
history:
- date: '2026-07-04'
  note: 'Generated by /spec-kitty.tasks for #584'
agent_profile: curator-carla
authoritative_surface: scripts/openclaw/agents/felix-admin-capture/
create_intent: []
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- scripts/openclaw/agents/felix-admin-capture/SOUL.md
- scripts/openclaw/agents/felix-admin-capture/USER.md
- scripts/openclaw/agents/felix-admin-capture/TOOLS.md
- scripts/openclaw/agents/felix-admin-capture/AGENTS.md
role: implementer
tags: []
---

# WP01 — Author capture workspace files

## ⚡ Do This First: Load Agent Profile

Before reading anything else, load your assigned agent profile so your identity,
governance scope, and boundaries are in force:

```
/ad-hoc-profile-load curator-carla
```

You are **curator-carla**, a knowledge-base and doctrine-maintenance specialist.
This work is exactly that: relocating agent-workspace content to its correct owner
file per a written standard, preserving meaning precisely. Honor that profile's
care-with-canonical-content boundaries for the rest of this work package.

## Objective

Re-author felix-admin-capture's `SOUL.md`, `USER.md`, and `TOOLS.md` into a clean,
self-contained set against the **OpenClaw Workspace Authoring Standard**
(`docs/design/openclaw-workspace-authoring-standard.md`, #587 — already merged to main),
and add the relocated label taxonomy to `AGENTS.md`.

**This is a PURE REFACTOR. Zero behavior change.** Every edit is a *relocation* or
*reduction* of existing content to its correct owner file — never a behavioral rewrite.
If an edit would change what capture actually does, it is out of scope. All
routing-intelligence enhancements are deferred to #651.

The authoritative move-table is in
[`research.md` Decision 1](../research.md); the content-block conservation model is in
[`data-model.md`](../data-model.md); the step-by-step runbook is in
[`quickstart.md`](../quickstart.md). Read those three before editing.

## Context — the four files and what already lives where

Verified current state (do not re-derive; confirm before editing):

- **AGENTS.md already owns**: the role (`## Authority`), the enforceable privacy rule
  (`## Privacy — absolute rule`), the Output Discipline block, and the Step 3
  `github_issue` route (labels "per heuristic"). **You only ADD the label taxonomy here.**
- **SOUL.md currently (wrongly) carries**: `## Purpose` (role), the full `## Privacy boundary`
  block + a renumber changelog parenthetical, and an ADD bullet in the voice section. Its
  voice section is the keeper.
- **USER.md currently (wrongly) carries**: a `## Date handling` block (TZ/ET-offset/no-Z) and
  an "ADD (managed)" fragment in Notes.
- **TOOLS.md currently (wrongly) carries**: the full `### Available Labels` taxonomy.

## Subtasks

### T001 — Author SOUL.md (voice/stance only)

**Purpose**: SOUL owns voice/stance only (#587 Principle 2). Strip everything that belongs elsewhere.

**Do**:
1. **Remove** the `## Purpose` / role block entirely. The role is already owned by AGENTS.md
   `## Authority` — this is a deletion, not a move (nothing to add elsewhere).
2. **Reduce** the full `## Privacy boundary` block to a single one-line behavioral **stance**,
   e.g. `## Privacy stance` → "I work only where I'm invited." Do **not** keep the enforceable
   path or the renumber-changelog parenthetical here (they belong in / already live in AGENTS/TOOLS).
3. **Remove** the ADD reference in the voice principles (the "structured and chunked … Kent has
   ADD" bullet). Keep the structural-chunking guidance itself if it is voice guidance, but drop
   the ADD attribution (see T002 rationale — ADD refs bias responses).
4. **Keep** the entire "Voice — write as Kent" section (principles, words to avoid, words that
   are Kent). This is SOUL's proper content — the keeper.

**Validation**: SOUL.md contains voice + a one-line privacy stance and nothing else. No
`## Purpose`, no enforceable privacy path, no changelog, no "ADD".

### T002 — Author USER.md (filtered person-view)

**Purpose**: USER is a filtered view of Kent scoped to interpreting inbox captures — not a
dossier, not operational mechanics (#587 Principle 4).

**Do**:
1. **Remove** the entire `## Date handling` section (TZ resolution, ET offset, no-Z rule). It
   relocates to TOOLS.md in T003 — do not lose the content; it must appear there.
2. **Remove** the "ADD (managed)" fragment from Notes. Rationale (Kent, 2026-07-03): ADD
   references bias the agent's responses; terseness is a property of the capture *method*, not
   a personal attribute.
3. **Keep** the filtered context (who Kent is, what he's building, that captures arrive via
   Wispr Flow / quick notes into `01-Inbox/`). **Add/keep** one neutral line attributing
   terseness to method: "Captures tend to be terse or fragmentary — voice or quick-note."

**Validation**: USER.md has name/address/timezone + a scoped Context + the neutral terseness
line. No date-handling mechanics, no "ADD".

### T003 — Author TOOLS.md (tool surface + relocated date-handling + label pointer)

**Purpose**: TOOLS owns the environment/tool surface and operational mechanics; lists that live
authoritatively elsewhere become pointers, not copies (#587 staleness-trap rule).

**Do**:
1. **Add** the date-handling mechanics relocated from USER.md (T001-of-USER): resolve dates in
   `America/New_York`; office2 runs UTC so use `TZ=America/New_York date`; include the ET offset
   (-04:00 EDT / -05:00 EST) on Vikunja `due_date`; never use the `Z` suffix. Place under a
   `## Date handling` section in TOOLS.
2. **Replace** the inlined `### Available Labels` taxonomy with a **pointer only** — a one-line
   reference to the canonical repository label source (the label taxonomy now lives in AGENTS.md
   beside Step 3, per T004; and the repo's label set is authoritative). Do NOT inline the list.
3. **Keep** the existing tool surface: vault paths, Vikunja API skill, GitHub CLI/skill, and the
   enforceable privacy path (`04-Growth/_private/` NEVER access) — the privacy path stays in TOOLS
   (FR-007).

**Validation**: TOOLS.md documents vault/vikunja/github/privacy + date-handling; the Available
Labels *list* is gone, replaced by a pointer; the `04-Growth/_private/` path remains.

### T004 — AGENTS.md: receive the Available Labels taxonomy (receiver only)

**Purpose**: Routing-time policy belongs beside the routing step (#587). The label taxonomy moves
from TOOLS into AGENTS beside the Step 3 `github_issue` route.

**Do**:
1. **Add** the Available Labels taxonomy (Priority+type, Area, Always-apply `spec: brief`) into
   `AGENTS.md` immediately beside the Step 3 `github_issue` route (which currently says "labels per
   heuristic").
2. **Change nothing else** in AGENTS.md. This is the ONLY permitted AGENTS.md edit (C-002). Do not
   touch the workflow, Output Discipline, privacy rule, or routing logic.

**Validation**: AGENTS.md Step 3 now carries the label taxonomy; `git diff` shows no other AGENTS.md
change.

### T005 — Validate against the #587 shared invariants

**Purpose**: Mechanical gate that the refactor preserved both shared invariants.

**Do**:
```bash
python3 -m scripts.openclaw.agents.validate_workspace --json
```
Confirm `felix-admin-capture` reports `ok: true` — Invariant A (privacy in AGENTS/TOOLS) and
Invariant B (Output Discipline in AGENTS) both PASS.

**Validation**: capture PASS. If privacy fails "only in SOUL.md", you stripped the enforceable rule
— restore it to AGENTS.md/TOOLS.md (FR-007). (Baseline: capture already PASSED before this WP; keep
it green.)

### T006 — Content-conservation check (no duplication, no loss)

**Purpose**: Prove each relocated block landed in exactly one place (NFR-002).

**Do** (from `scripts/openclaw/agents/felix-admin-capture/`):
```bash
grep -l "Date handling" USER.md TOOLS.md        # expect: TOOLS.md only
grep -c "Available Labels" AGENTS.md TOOLS.md    # expect: AGENTS.md >=1 ; TOOLS.md 0 (pointer text differs)
grep -ci "ADD" SOUL.md USER.md                   # expect: 0 0  (no ADD attribution)
grep -l "04-Growth/_private" AGENTS.md TOOLS.md  # expect: both present
grep -c "## Purpose" SOUL.md                     # expect: 0
```

**Validation**: every expectation holds.

## Post-merge acceptance (FR-009, FR-010, FR-011 — operator-owned, NOT part of this WP's lane)

These three FRs are satisfied **after** the feature branch merges to main, because capture's
prompt files deploy via the automatic **agent-prompt-sync** pull pipeline (`deploy_agent_prompts.py`,
#567/#136), which fires only on `git pull --ff-only origin main`. They therefore **cannot** run
during this WP's implement/review on the feature branch. **No `deploys/queued/` manifest is
authored** — agent-prompt files are outside felix-deployer's scope (the #636 boundary,
[research.md Decision 4](../research.md)).

The full, runnable procedure lives in [quickstart.md](../quickstart.md) §4–9. Summary of the
operator-owned post-merge acceptance (mirrors the #325 canary precedent):

- **Baseline (before merge)**: capture capture's current routing decisions (read-only office2
  `prescan` + routing snapshot) to compare against post-deploy.
- **FR-009 (deploy)**: after merge to main, confirm the agent-prompt-sync timer is live and that
  `/data/services/openclaw/deploy/agent-prompt-sync.jsonl` records the copy of each changed file.
- **FR-010 (parity)**: per-file `md5sum` match between office2 (`/data/services/openclaw/inbox-agent/`)
  and repo.
- **FR-011 (smoke)**: re-run capture on the baseline inputs; routing decisions identical → PASS;
  divergence → rollback (`git revert` the merge → re-merge → sync re-copies within 5 min).
- **Rebaseline**: expected "not required — agent prompt files not hashed by the monitor (#621 gap)".

The reviewer judges the WP on the **in-repo authoring** (T001–T006); the post-merge steps are the
operator's to execute and record on the merge, not a lane deliverable.

## Branch Strategy

Planning artifacts were generated on `feat/author-capture-workspace`. This WP's completed
changes merge back into `feat/author-capture-workspace` (the feature branch → main is the
separate final PR). Execution worktrees are allocated per computed lane from `lanes.json`
during `/spec-kitty.implement`; do not create worktrees by hand.

## Definition of Done

- [ ] SOUL/USER/TOOLS authored per the move-table; AGENTS.md gains only the label taxonomy.
- [ ] `validate_workspace.py` reports `felix-admin-capture` PASS (both invariants).
- [ ] All T006 conservation greps hold (each moved block in exactly one place; ADD gone; privacy retained).
- [ ] `git diff` on AGENTS.md shows the label receiver and nothing else (C-002).
- [ ] No behavioral rewording — a reviewer can trace every change to a move-table entry.

## Risks & Reviewer Guidance

- **FR-007 (highest risk)**: reducing SOUL's privacy to a stance must NOT remove the enforceable
  rule from the workspace — it stays in AGENTS.md/TOOLS.md. Reviewer: confirm `validate_workspace.py`
  PASS and the `04-Growth/_private/` grep hits AGENTS + TOOLS.
- **C-002 scope**: AGENTS.md must show *only* the label receiver. Reviewer: read the AGENTS.md diff
  line-by-line.
- **Behavior preservation**: this is a refactor. Reviewer: every content change must map to a
  move-table row (research.md Decision 1); flag any net-new or reworded instruction.
- **Staleness trap**: TOOLS.md must have a *pointer*, not a copy, of the label list.

## Activity Log

- 2026-07-04T18:41:41Z – claude:sonnet:curator-carla:implementer – shell_pid=8251 – Assigned agent via action command
- 2026-07-04T18:46:50Z – claude:sonnet:curator-carla:implementer – shell_pid=8251 – Ready for review — validator PASS, conservation greps hold, AGENTS.md label-receiver-only
- 2026-07-04T18:47:34Z – codex:gpt-5:reviewer-renata:reviewer – shell_pid=10541 – Started review via action command
- 2026-07-04T18:51:05Z – user – shell_pid=10541 – Moved to planned
- 2026-07-04T19:05:54Z – user – shell_pid=10541 – Arbiter approval (orchestrator): WP01 content verified correct — capture PASSES both #587 invariants and all conservation greps, confirmed by running validate_workspace.py from main against the lane files. Codex cycle-1 rejection was environmental (mission branches predate the #587 merge onto target feat/author-capture-workspace, so the validator is absent from the isolated lane); validator+edits combine at merge, re-confirmed in-branch post-merge.
- 2026-07-04T19:09:46Z – user – shell_pid=10541 – Re-review to produce an approved review artifact (prior codex rejection was environmental — validator absent from isolated lane; content verified PASS by arbiter).
- 2026-07-04T19:09:50Z – claude:opus:reviewer-renata:reviewer – shell_pid=17721 – Started review via action command
- 2026-07-04T19:10:13Z – user – shell_pid=17721 – Review passed (arbiter, opus): capture workspace refactor verified correct. Ran the #587 validator (validate_workspace.py) from the main checkout against the lane's edited files: felix-admin-capture PASSES both invariants (privacy in AGENTS+TOOLS; Output Discipline in AGENTS). Conservation checks hold: Date handling in TOOLS only; ADD removed from SOUL+USER; ## Purpose removed from SOUL; 04-Growth/_private retained in AGENTS+TOOLS; Available Labels moved to AGENTS with only a pointer left in TOOLS; AGENTS diff is the label receiver only (C-002). The prior codex cycle-2 rejection was environmental (validator module absent from the isolated lane because the mission branches predate the #587 merge onto the target branch); validator+edits combine at merge.
- 2026-07-04T19:15:53Z – user – shell_pid=17721 – Validator now present in-lane (#587 vendored into mission base); re-review so it runs in place.
- 2026-07-04T19:16:07Z – codex:gpt-5:reviewer-renata:reviewer – shell_pid=19998 – Started review via action command
- 2026-07-04T19:18:06Z – user – shell_pid=19998 – Review passed: felix-admin-capture validator PASS; conservation greps hold; AGENTS.md change limited to label receiver; no contracts or shared-file conflicts
