{"id":"DEC-001","question":"Should shared Felix agent prompt context (SOUL/USER/TOOLS) be factored out of per-agent workspaces into a shared/symlinked/composed source?","answer":"No. Keep each agent workspace self-contained; per-agent SOUL/USER/TOOLS are canonical customization surfaces, not accidental duplication. Control drift via workspace reconciliation + targeted CI lint, not deduplication.","status":"settled","date":"2026-06-11","invariants_touched":["INV-004"],"rationale":"OpenClaw intentionally treats each configured agent workspace as self-contained; per-agent files force each agent to own its perspective on shared truths and make boundaries visible at the agent level. Actionable authoring moved to #167.","source_issue":553}
{"id":"DEC-002","question":"How should Felix treat model-asserted claims about system/infrastructure state (job status, completion, health)?","answer":"Never trust them as fact. Deterministic state is produced by deterministic checks; the two-layer / no-fabrication rule is a fleet-wide invariant (INV-001), not a per-agent patch. Model output is judgment over plumbing-supplied facts.","status":"settled","date":"2026-07-06","invariants_touched":["INV-001"],"rationale":"Recurring fabrication class: #562 (bare-python cron false-fail), #662 (sonnet misdiagnosis-and-reversal), #683 (main:sonnet fabricated a completion status + created an unrequested cron). Motivates Foundation 1 deterministic observability.","source_issue":683}
{"id":"DEC-003","question":"Should Felix adopt OpenTelemetry (#124) as part of Bedrock stabilization?","answer":"No — deferred. Out of stabilization scope; Foundation 1 observability uses deterministic canaries + a token/usage ledger, not OTel. Revisit post-stabilization only if scale warrants.","status":"settled","date":"2026-07-06","invariants_touched":[],"rationale":"Anti-over-engineering (engineering principle #10, guardrail preference). OTel is heavyweight relative to single-operator scale; deterministic checks meet the observability need now.","source_issue":124}
{"id":"DEC-004","question":"Should the F3 coherence substrate be built on a knowledge-graph / Obsidian Canvas (#644/#645/#646) now?","answer":"No — deferred. Coherence runs on hand-curated flat files (doctrine.md + decisions.jsonl). The graph/vector substrate is the second-brain (learning-the-human) domain, a post-stabilization roadmap item; design F3 graph-ready so the corpus is its seed.","status":"settled","date":"2026-07-06","invariants_touched":[],"rationale":"Two knowledge domains, two right-answers: doctrine is small/stable/human-authored -> flat files are correct; the second brain is large/relational/dynamic -> a graph wins. A graph for ~dozens of invariants is over-engineering.","source_issue":646}
{"id":"DEC-005","question":"How much of spec-kitty's doctrine/governance engine should Felix adopt?","answer":"Borrow the shapes (flat files + tiny deterministic helpers: scoped invariants, decision markers, a coherence pre-scan, a significance gate). Do NOT borrow the services: DoctrineService, decision-review-graph, 9 artifact kinds, 5-tier resolver, event stream, lamport clocks, SaaS sync.","status":"settled","date":"2026-07-06","invariants_touched":[],"rationale":"Single-operator scale + deterministic-first posture. spec-kitty's engine never calls an LLM (inert data; Python selects/records); the borrowable core is three flat files + two tiny helpers, and the service machinery is over-engineering for Felix.","source_issue":673}
{"id":"DEC-006","question":"What standard of verification is required before a fix or capability is declared done/closed?","answer":"Behavioral end-to-end verification. Static signals (prompt text, config, greps, mocked unit tests) are necessary but never sufficient; the real path must be exercised and its real outcome observed before declaring done, closing an issue, or reporting completion. If the path cannot be run, say so — do not infer success.","status":"settled","date":"2026-07-08","invariants_touched":["INV-006"],"rationale":"Recurring myopia: #662 (haiku->sonnet fix diagnosed from static evidence, later reversed), #679 (research validated static signals but never ran the path e2e; capture->calendar silently dead ~since Jun 11), #683 (main:sonnet fabricated a completion status). Surfaced by the first F3 point-cut dry-run against #662 on 2026-07-08.","source_issue":679}
{"id":"DEC-007","question":"Should the component-health canary registry (#327) extend felix-trust-scan, and how must it surface alerts?","answer":"Ship it as a SIBLING scanner — a new scripts/canary/ package + its own felix-canary systemd-user timer (15-min) — that shares the #701 alert-bus emit library rather than extending felix-trust-scan. It emits ONLY through the one canonical #701 felix-alert bus (INV-003), never a per-component notification path; the deterministic ntfy canary is the must-always backstop.","status":"settled","date":"2026-07-11","invariants_touched":["INV-003"],"rationale":"Single-responsibility: felix-trust-scan watches TRUST drift (rogue crons, fabricated completions), a different domain from component HEALTH; folding health-watching into it would couple the cadence + lifecycle of two unrelated concerns. Two sibling scanners sharing the emit substrate stay simple and independently evolvable. As a new alert producer the canary forward-binds to INV-003 (mechanism built by F1/#516). Mirrors research R1 + operator decision DM-01KX8TY1KRNXQ2Y7C6QKXYEYCR.","source_issue":327}
